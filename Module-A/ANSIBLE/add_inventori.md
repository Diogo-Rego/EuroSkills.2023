create the ansible config directory
```
mkdir /etc/ansible 
```
make the config file 
```
nano /etc/ansible/ansible.cfg
```
ansible configuration 
```
[defaults]
inventory = < the inventory path >
```